spring.application.name=check-in-system

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/miningindaba
spring.datasource.username=postgres
spring.datasource.password=Margot+250793833800
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Server Configuration
server.port=8080

# Logging
logging.level.com.mobile_app.check_in_system=DEBUG
logging.level.org.springframework.security=DEBUG

# CORS Configuration for React Native
spring.web.cors.allowed-origins=*
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
